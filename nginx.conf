server {
    listen 80;
    server_name _;

    # The root directory for your built files
    root /usr/share/nginx/html;
    index index.html;

    # Try to serve the exact file; fallback to index.html for SPA routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}
